
To build on Linux, you must have mono installed (aka Xamarin).
In the same directory as the one this file is in, run:

msbuild SudokuLinux.sln

To build on Windows, open Sudoku.sln with Visual Studio 2017 or later
and build the solution. Alternatively, run:
msbuild.exe Sudoku.sln

Enjoy!

