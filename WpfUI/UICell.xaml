<UserControl x:Class="WpfUI.UICell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="{Binding Background}">
            <TextBlock Text="{Binding Number}" Foreground="{Binding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="33"/>
        </Border>
        <TextBlock Text="{Binding Possibles}" Foreground="{Binding PossiblesForeground}" Margin="4,2,0,0" FontSize="16"/>
    </Grid>
</UserControl>
